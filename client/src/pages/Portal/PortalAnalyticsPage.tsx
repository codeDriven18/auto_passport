import { PortalWorkspacePlaceholder } from './PortalWorkspacePlaceholder';

export function PortalAnalyticsPage() {
  return (
    <PortalWorkspacePlaceholder
      title="Analytics"
      subtitle="Applications, views, saves, interviews, offers, hires, response times, and conversion funnel."
      ctaLabel="Go to dashboard"
      ctaTo="/portal"
    />
  );
}
