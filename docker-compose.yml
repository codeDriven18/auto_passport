version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: swipejobs
    volumes:
      - swipejobs_pg:/var/lib/postgresql/data

volumes:
  swipejobs_pg:
